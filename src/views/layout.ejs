<!DOCTYPE html>
<html class="dark" lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="csrf-token" content="<%= csrfToken %>" />
  <meta name="theme-color" content="#4f46e5" />
  <title><%= title %></title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Plus+Jakarta+Sans:wght@700;800&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet" />

  <script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
  <script>
    tailwind.config = {
      darkMode: "class",
      theme: {
        extend: {
          colors: {
            primary: "#6366f1",
            "background-light": "#f8fafc",
            "background-dark": "#0a0a0f",
            "surface-dark": "#161621",
            "accent-purple": "#8b5cf6"
          },
          fontFamily: {
            sans: ["Inter", "sans-serif"],
            display: ["Plus Jakarta Sans", "sans-serif"]
          },
          borderRadius: {
            DEFAULT: "0.75rem",
            "2xl": "1.5rem"
          }
        }
      }
    };
  </script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .glass {
      background: rgba(255, 255, 255, 0.03);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(255, 255, 255, 0.08);
    }
    .hero-gradient {
      background: radial-gradient(circle at top left, rgba(99, 102, 241, 0.15), transparent),
                  radial-gradient(circle at bottom right, rgba(139, 92, 246, 0.1), transparent);
    }
    .purple-glow {
      box-shadow: 0 0 20px rgba(99, 102, 241, 0.2);
    }
    .animate-float {
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    :root{
      --bg0:#070a13;
      --bg1:#0b1022;
      --bg2:#0b1224;
      --card: rgba(15, 23, 42, .72);
      --card2: rgba(15, 23, 42, .55);
      --stroke: rgba(148, 163, 184, .18);
      --stroke2: rgba(148, 163, 184, .10);
      --text: #e5e7eb;
      --muted: rgba(226,232,240,.72);
      --primary:#5865f2;
      --primary2:#4f46e5;
      --danger:#ef4444;
      --warning:#f59e0b;
      --success:#22c55e;
      --shadow: 0 18px 60px rgba(0,0,0,.45);
      --r12: 12px;
      --r16: 16px;
      --r20: 20px;
    }
    .small{ font-size:.9rem; }
    .muted, .text-muted{ color: var(--muted) !important; }
    b,strong{ font-weight: 700; }

    .btn{
      border-radius: 14px;
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.35);
      color: var(--text);
      font-size: .95rem;
      padding: 9px 16px;
      transition: transform .12s ease, background .12s ease, border-color .12s ease, box-shadow .12s ease;
    }
    .btn-sm{
      padding: 7px 13px;
      font-size: .9rem;
    }
    .btn:hover{
      transform: translateY(-1px);
      box-shadow: 0 6px 18px rgba(15,23,42,.55);
    }
    .btn:active{ transform: translateY(0); }
    .btn-pill{ border-radius: 999px !important; }
    .btn-primary{
      background: linear-gradient(90deg, var(--primary2), var(--primary));
      border-color: rgba(88,101,242,.35);
      color: white;
    }
    .btn-outline-primary{
      background: transparent;
      border-color: rgba(88,101,242,.35);
      color: #c7d2fe;
    }
    .btn-outline-dark{
      background: rgba(15,23,42,.2);
      color: rgba(226,232,240,.92);
    }
    .btn-secondary{
      background: rgba(148,163,184,.12);
      border-color: rgba(148,163,184,.25);
    }
    .btn-danger{
      background: rgba(239,68,68,.14);
      border-color: rgba(239,68,68,.30);
      color: #fecaca;
    }
    .btn-warning{
      background: rgba(245,158,11,.14);
      border-color: rgba(245,158,11,.32);
      color: #fde68a;
    }
    .btn-cancel{
      background: #ef4444;
      border-color: #ef4444;
      color: #fff;
    }
    .btn-cancel:hover{
      background: #dc2626;
      border-color: #dc2626;
      color: #fff;
    }
    .btn-cancel i{ color: inherit; }
    .btn-success{
      background: rgba(34,197,94,.14);
      border-color: rgba(34,197,94,.30);
      color: #bbf7d0;
    }
    .btn-icon{
      width: 44px; height: 44px;
      display:grid; place-items:center;
      padding:0;
      border-radius: 14px;
    }
    .btn i{ margin-right: 8px; }
    .btn.btn-icon i{ margin-right: 0; }

    .cases-grid{
      display:grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 360px));
      gap: 18px;
      justify-content: flex-start;
    }
    @media (max-width: 992px){
      .cases-grid{ grid-template-columns: repeat(auto-fill, minmax(260px, 340px)); }
    }
    @media (max-width: 560px){
      .cases-grid{ grid-template-columns: minmax(260px, 1fr); }
    }
    .case-card{
      border-radius: var(--r20);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.50);
      overflow:hidden;
      box-shadow: 0 18px 55px rgba(0,0,0,.32);
      display:flex;
      flex-direction:column;
      min-height: 320px;
    }
    .case-card.disabled{ opacity: .65; filter: grayscale(.15); }
    .case-media{ height: 170px; position:relative; }
    .case-img{
      width:100%; height:100%; object-fit: cover;
      display:block;
    }
    .case-img--placeholder{
      background: linear-gradient(135deg, rgba(88,101,242,.25), rgba(168,85,247,.20));
    }
    .case-content{ padding: 14px 14px 16px; display:flex; flex-direction:column; gap: 10px; flex: 1; }
    .case-title{ margin:0; font-weight: 900; font-size: 1.2rem; }
    .case-badges{ display:flex; gap: 6px; flex-wrap:wrap; margin-top: 6px; }
    .chip{
      font-size: .82rem;
      color: rgba(226,232,240,.86);
      padding: 5px 10px;
      border-radius: 999px;
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.40);
    }
    .chip--primary{
      border-color: rgba(88,101,242,.35);
      background: rgba(88,101,242,.14);
      color: #c7d2fe;
    }
    .case-meta{ display:flex; gap: 10px; flex-wrap:wrap; color: rgba(226,232,240,.78); }
    .case-actions{ margin-top: auto; display:flex; justify-content:flex-end; }
    .case-actions .btn{ display:inline-flex; align-items:center; gap: 10px; }
    .case-actions .btn i{ margin-right: 0; }

    .case-prizes{
      display:flex; gap: 6px; flex-wrap:wrap;
      margin: 10px 0 6px;
    }
    .case-prize{
      padding: 5px 10px; border-radius: 999px;
      font-size: .82rem; font-weight: 800;
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.40);
    }
    .case-prize.rarity-rare{
      border-color: rgba(59,130,246,.35);
      background: rgba(59,130,246,.14);
      color: #c7d2fe;
    }
    .case-prize.rarity-epic{
      border-color: rgba(168,85,247,.35);
      background: rgba(168,85,247,.16);
      color: #e9d5ff;
    }
    .case-prize.rarity-mythic{
      border-color: rgba(234,179,8,.35);
      background: rgba(234,179,8,.14);
      color: #fde68a;
    }
    .case-prize.rarity-legendary{
      border-color: rgba(244,63,94,.35);
      background: rgba(244,63,94,.14);
      color: #fecaca;
    }
    .tabs{
      display:flex;
      gap: 10px;
      margin: 12px 0 16px;
      flex-wrap:wrap;
    }
    .tab{
      padding: 11px 18px;
      border-radius: 999px;
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.35);
      color: rgba(226,232,240,.9);
      font-weight: 800;
      font-size: .9rem;
      cursor:pointer;
    }
    .tab.active{
      border-color: rgba(88,101,242,.35);
      background: rgba(88,101,242,.16);
      color:#c7d2fe;
    }
    .tab-content{ display:none; }
    .tab-content.active{ display:block; }

    .user-card{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 16px;
      padding: 16px 18px;
      border-radius: var(--r20);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.55);
      margin-bottom: 12px;
    }
    .user-meta{ display:flex; align-items:center; gap: 12px; min-width: 0; }
    .user-text{ min-width: 0; }
    .user-name{
      font-weight: 900;
      white-space:nowrap;
      overflow:hidden;
      text-overflow:ellipsis;
      max-width: 320px;
    }
    .user-avatar{
      width: 44px; height:44px;
      border-radius: 16px;
      object-fit: cover;
      border: 1px solid var(--stroke2);
    }
    .user-avatar--placeholder{
      background: rgba(148,163,184,.15);
    }
    .user-actions{
      display:flex;
      gap: 12px;
      flex-wrap:wrap;
      justify-content:flex-end;
    }

    .user-detail{
      display:grid;
      grid-template-columns: 1.05fr .95fr;
      gap: 16px;
    }
    @media (max-width: 900px){
      .user-detail{ grid-template-columns: 1fr; }
    }
    .user-detail__top{
      display:flex;
      align-items:flex-start;
      justify-content:space-between;
      gap: 14px;
    }
    .user-detail__avatar{
      width: 86px; height:86px;
      border-radius: 22px;
      object-fit: cover;
      border: 1px solid var(--stroke2);
    }
    .user-detail__stats{
      display:flex; gap: 12px; margin-top: 12px; flex-wrap:wrap;
    }
    .stat-mini{
      padding: 10px 12px;
      border-radius: var(--r16);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.38);
      min-width: 160px;
      display:flex; flex-direction:column;
    }
    .stat-mini b{ font-size: 1.15rem; }
    .user-detail__controls{ margin-top: 12px; display:flex; flex-direction:column; gap: 12px; }
    .level-line{ display:flex; gap: 10px; align-items:center; flex-wrap:wrap; }
    .level-hint{ font-size: .86rem; }
    .balance-line{ display:flex; gap: 10px; align-items:center; flex-wrap:wrap; }

    .prizes-box{
      border-radius: var(--r20);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.35);
      padding: 14px;
    }
    .prizes-head{
      display:flex; align-items:baseline; justify-content:space-between; gap: 10px;
      padding-bottom: 10px;
      border-bottom: 1px solid var(--stroke2);
      margin-bottom: 10px;
    }
    .prize-row{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
      padding: 12px;
      border-radius: var(--r16);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.28);
      margin-bottom: 10px;
    }
    .prize-title{ font-weight: 800; }
    .prize-right{ display:flex; gap: 10px; align-items:center; flex-wrap:wrap; justify-content:flex-end; }
    .status-pill{
      padding: 6px 10px;
      border-radius: 999px;
      border: 1px solid var(--stroke2);
      background: rgba(148,163,184,.10);
      color: rgba(226,232,240,.85);
      font-weight: 800;
      font-size: .84rem;
    }
    .status-pill--ok{
      border-color: rgba(34,197,94,.35);
      background: rgba(34,197,94,.12);
      color: #bbf7d0;
    }
    .empty-inline{
      color: rgba(226,232,240,.75);
      padding: 10px 12px;
      border-radius: var(--r16);
      border: 1px dashed rgba(148,163,184,.22);
    }

    .admin-case{
      display:grid;
      grid-template-columns: 260px 1fr;
      gap: 18px;
      padding: 18px 20px;
      border-radius: var(--r20);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.55);
      margin-bottom: 14px;
    }
    @media (max-width: 900px){
      .admin-case{ grid-template-columns: 1fr; }
    }
    .admin-case__media{
      position:relative;
      border-radius: var(--r16);
      overflow:hidden;
      min-height: 110px;
    }
    .admin-case__img{
      width:100%; height:100%;
      object-fit: cover;
      display:block;
    }
    .admin-case__img--placeholder{
      height: 100%;
      background: linear-gradient(135deg, rgba(88,101,242,.25), rgba(168,85,247,.20));
    }
    .admin-case__name{
      position:absolute;
      left: 10px; right: 10px; bottom: 10px;
      padding: 8px 10px;
      border-radius: 14px;
      background: rgba(0,0,0,.35);
      border: 1px solid rgba(255,255,255,.14);
      backdrop-filter: blur(8px);
      font-weight: 900;
    }
    .admin-case__meta{
      display:flex; gap: 10px; flex-wrap:wrap;
      color: rgba(226,232,240,.78);
    }
    .admin-case__actions{
      margin-top: 14px;
      display:flex;
      justify-content:flex-end;
      gap: 12px;
      flex-wrap:wrap;
    }

    .prize-fields{ display:flex; flex-direction:column; gap: 10px; }
    .prize-row-edit{
      display:grid;
      grid-template-columns: 1.2fr .55fr .6fr 1fr 54px;
      gap: 10px;
    }
    @media (max-width: 900px){
      .prize-row-edit{ grid-template-columns: 1fr 1fr; }
      .prize-row-edit .btn-icon{ grid-column: 2; justify-self:end; }
    }
    .form-control{
      background: rgba(15,23,42,.35) !important;
      border: 1px solid var(--stroke2) !important;
      color: rgba(226,232,240,.92) !important;
      border-radius: 14px !important;
      padding: 12px 12px !important;
      caret-color: rgba(226,232,240,.92);
    }
    input.form-control, textarea.form-control, select.form-control{
      background-color: rgba(15,23,42,.35) !important;
    }
    input[type="number"].form-control{
      background-color: rgba(15,23,42,.35) !important;
    }
    .form-control:-webkit-autofill{
      -webkit-box-shadow: 0 0 0px 1000px rgba(15,23,42,.35) inset;
      -webkit-text-fill-color: rgba(226,232,240,.92);
    }
    .form-control::placeholder{ color: rgba(226,232,240,.45); }
    .form-actions{
      margin-top: 32px;
      display:flex;
      gap: 10px;
      flex-wrap:wrap;
    }
    .form-modern .row{
      display:grid;
      grid-template-columns: repeat(12, minmax(0, 1fr));
      column-gap: 18px;
      row-gap: 14px;
    }
    .form-modern .col-md-7{ grid-column: span 7; }
    .form-modern .col-md-5{ grid-column: span 5; }
    .form-modern .col-md-4{ grid-column: span 4; }
    .form-modern .col-12{ grid-column: 1 / -1; }
    @media (max-width: 900px){
      .form-modern .row{
        grid-template-columns: 1fr;
      }
      .form-modern .col-md-7,
      .form-modern .col-md-5,
      .form-modern .col-md-4,
      .form-modern .col-12{
        grid-column: auto;
      }
    }
    .form-check.form-switch{
      display:flex;
      align-items:center;
      gap: 8px;
      padding-left: 0;
    }
    .form-check.form-switch .form-check-input{
      margin-left: 0;
    }
    .form-check.form-switch .form-check-label{
      margin-left: 0;
      line-height: 1.2;
      padding-top: 2px;
    }
    .form-switch .form-check-input{
      width: 46px;
      height: 24px;
      cursor:pointer;
      background-color: rgba(148,163,184,.18);
      border-color: rgba(148,163,184,.25);
    }
    .form-switch .form-check-input:checked{
      background-color: rgba(88,101,242,.60);
      border-color: rgba(88,101,242,.55);
    }

    .empty-state{
      padding: 18px;
      border-radius: var(--r20);
      border: 1px dashed rgba(148,163,184,.28);
      background: rgba(15,23,42,.28);
      text-align:center;
    }
    .empty-state--small{ padding: 14px; text-align:left; }
    .empty-emoji{ font-size: 1.5rem; margin-bottom: 6px; }
    .empty-title{ font-weight: 900; }
    .empty-sub{ color: rgba(226,232,240,.70); margin-top: 4px; }

    .spinner-track{
      margin-top: 10px;
      border-radius: var(--r20);
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.35);
      overflow:hidden;
      height: 70px;
    }
    .spinner-reel{
      display:flex;
      flex-direction:row;
      transform: translateX(0);
      will-change: transform;
    }
    .reel-item{
      min-width: 140px;
      height: 70px;
      display:flex; align-items:center;
      padding: 0 16px;
      border-right: 1px solid rgba(148,163,184,.10);
      color: rgba(226,232,240,.92);
      font-weight: 800;
    }
    .spinner-reel.spin{
      animation: spin 5.2s cubic-bezier(.08,.7,.2,1) forwards;
    }
    @keyframes spin{
      to{ transform: translateX(calc(var(--spin-offset, 0px) * -1)); }
    }
    .spinner-pointer{
      position:absolute;
      left: 50%;
      top: 0;
      transform: translateX(-50%);
      width: 0; height: 0;
      border-left: 8px solid transparent;
      border-right: 8px solid transparent;
      border-top: 12px solid #c7d2fe;
      filter: drop-shadow(0 2px 6px rgba(99,102,241,.35));
      z-index: 2;
    }
    .win-card{
      margin-top: 12px;
      display:flex; align-items:center; gap: 10px;
      padding: 12px 14px;
      border-radius: var(--r16);
      border: 1px solid rgba(34,197,94,.30);
      background: rgba(34,197,94,.10);
    }
    .win-emoji{ font-size: 1.6rem; }
    .win-title{ color: rgba(226,232,240,.75); font-weight: 800; font-size: .9rem; }
    .win-name{ font-weight: 900; font-size: 1.1rem; }

    .spinner-prizes-panel.hidden{ display:none; }
    .prizes-list-inline{ display:flex; flex-wrap:wrap; gap: 10px; }
    .spinner-prizes-panel .case-prize{
      display:inline-flex; align-items:center; gap:8px; padding: 6px 12px;
    }
    .case-prize__emoji{
      width: 20px; height: 20px; border-radius: 6px;
      background: rgba(148,163,184,.12);
      display:flex; align-items:center; justify-content:center;
      overflow:hidden;
    }
    .case-prize__emoji img{ width:100%; height:100%; object-fit:cover; display:block; }
    .reel-item__emoji{ display:inline-flex; align-items:center; justify-content:center; width:20px; height:20px; border-radius:6px; background: rgba(148,163,184,.12); margin-right:8px; overflow:hidden; }
    .reel-item__emoji-img{ width:100%; height:100%; object-fit:cover; display:block; }
    .hide-scrollbar{ -ms-overflow-style: none; scrollbar-width: none; }
    .hide-scrollbar::-webkit-scrollbar{ display: none; }
    @media (max-width: 480px){
      #case-spinner{ padding: 10px; }
      #case-spinner .w-full.max-w-xl{ max-width: 100%; }
      .spinner-track{ height: 56px; }
      .reel-item{ min-width: 110px; height: 56px; padding: 0 10px; font-size: .9rem; }
      .spinner-pointer{ border-left-width: 6px; border-right-width: 6px; border-top-width: 10px; }
      .win-card{ padding: 10px 12px; }
      .win-name{ font-size: 1rem; }
      .prizes-list-inline{ gap: 8px; }
      #show-prizes{ padding: 6px 10px; font-size: .85rem; }
    }

    .particle-layer{
      position: fixed;
      inset: 0;
      pointer-events: none;
      z-index: 5;
    }
    .particle-box{
      position: absolute;
      top: -40px;
      color: rgba(226,232,240,.25);
      animation: fall linear forwards;
      will-change: transform, opacity;
      font-size: 18px;
    }
    @keyframes fall{
      0%{ transform: translate3d(0, -40px, 0) rotate(0deg); opacity: .9; }
      100%{ transform: translate3d(var(--drift, 0px), 110vh, 0) rotate(180deg); opacity: 0.4; }
    }

    .case-error{
      margin-top: 14px;
      display:flex;
      align-items:flex-start;
      gap: 10px;
      padding: 12px 14px;
      border-radius: var(--r16);
      border: 1px solid rgba(239,68,68,.35);
      background: radial-gradient(circle at top left, rgba(248,113,113,.25), transparent),
                  rgba(15,23,42,.85);
    }
    .case-error__icon{
      width: 32px;
      height: 32px;
      border-radius: 999px;
      display:flex;
      align-items:center;
      justify-content:center;
      background: rgba(248,113,113,.12);
      font-size: 18px;
    }
    .case-error__body{
      display:flex;
      flex-direction:column;
      gap: 2px;
    }
    .case-error__title{
      font-weight: 800;
      color: #fecaca;
      font-size: .95rem;
    }
    .case-error__text{
      font-size: .86rem;
      color: rgba(248,250,252,.85);
    }

    .pending-progress{
      position:relative;
      width:100%;
      height: 6px;
      border-radius: 999px;
      background: rgba(148,163,184,.22);
      overflow:hidden;
    }
    .pending-progress__bar{
      position:absolute;
      inset:0;
      width:40%;
      background: linear-gradient(90deg, var(--primary2), var(--primary), #a855f7);
      box-shadow: 0 0 12px rgba(88,101,242,.5);
      transform: translateX(-40%);
      animation: pending-slide 1.4s ease-in-out infinite;
    }
    @keyframes pending-slide{
      0%{ transform: translateX(-40%); }
      50%{ transform: translateX(15%); }
      100%{ transform: translateX(110%); }
    }

    .modal-content{
      position:relative;
      width:100%;
      max-width: 820px;
      border-radius: 24px;
      border: 1px solid var(--stroke);
      background: var(--card);
      box-shadow: var(--shadow);
      padding: 22px 56px 24px 24px;
      max-height: calc(100vh - 80px);
      overflow-y:auto;
    }
    .modal-content--wide{
      max-width: 1040px;
    }
    .modal.open .modal-content{
      animation: modal-in .16s ease-out;
    }
    @keyframes modal-in{
      from{
        transform: translateY(8px) scale(.97);
        opacity: 0;
      }
      to{
        transform: translateY(0) scale(1);
        opacity: 1;
      }
    }
    .modal-close{
      position:absolute;
      top: 14px;
      right: 16px;
      width: 32px;
      height: 32px;
      border-radius: 999px;
      border: 1px solid var(--stroke2);
      background: rgba(15,23,42,.75);
      color: var(--muted);
      display:flex;
      align-items:center;
      justify-content:center;
      cursor:pointer;
      font-size: 20px;
      line-height: 1;
      z-index: 20;
    }
    .modal-close:hover{
      background: rgba(15,23,42,.95);
      color: #e5e7eb;
    }
    .modal-head{
      padding-bottom: 16px;
      margin-bottom: 18px;
      border-bottom: 1px solid var(--stroke2);
    }
    .modal-head--sticky{
      position:sticky;
      top: 12px;
      z-index: 5;
      margin: 0 -24px 20px;
      padding: 16px 24px 16px;
      border-bottom: 1px solid var(--stroke2);
      background: linear-gradient(to bottom, rgba(15,23,42,.96), rgba(15,23,42,.92));
    }
    .modal-title{
      display:flex;
      align-items:center;
      gap: 12px;
    }
    .modal-title__icon{
      width: 40px;
      height: 40px;
      border-radius: 16px;
      display:flex;
      align-items:center;
      justify-content:center;
      background: rgba(88,101,242,.16);
      border: 1px solid rgba(88,101,242,.30);
      color: #c7d2fe;
      font-size: 20px;
    }
    .cases-header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 16px;
      margin-bottom: 16px;
    }
    .cases-header__left{
      flex: 1;
      min-width: 0;
    }
    .cases-header__right{
      flex-shrink:0;
      display:flex;
      align-items:center;
      justify-content:flex-end;
      margin-right: 0;
    }
    .cases-admin-list{
      margin-top: 8px;
      display:flex;
      flex-direction:column;
      gap: 10px;
    }

    html.modal-open, html.modal-open body{
      overflow: hidden !important;
    }
    .modal{
      position: fixed;
      inset: 0;
      display:none;
      align-items:center;
      justify-content:center;
      padding: 18px;
      background: rgba(0,0,0,.55);
      z-index: 1000;
    }
    .modal.open{ display:flex; }
  </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen selection:bg-primary selection:text-white">
  <% const viewUser = typeof user !== 'undefined' ? user : null; %>

  <% if (showNav) { %>
  <nav class="sticky top-0 z-50 px-6 py-4 glass border-b border-white/5 mb-6">
    <div class="max-w-7xl mx-auto flex items-center justify-between">
      <div class="flex items-center gap-8">
        <a href="/app" class="flex items-center gap-2 group cursor-pointer">
          <div class="bg-primary p-2 rounded-xl text-white shadow-lg shadow-primary/20 group-hover:scale-105 transition-transform">
            <span class="material-icons-round">inventory_2</span>
          </div>
          <span class="font-display text-xl font-bold tracking-tight">
            Кейсики
            <span class="text-[10px] uppercase tracking-widest bg-primary/20 text-primary px-1.5 py-0.5 rounded ml-1 align-middle">Beta</span>
          </span>
        </a>
        <div class="hidden md:flex items-center gap-1 bg-black/20 p-1 rounded-full border border-white/5">
          <a href="/app" class="px-5 py-2 rounded-full text-sm flex items-center gap-2 min-w-max <%= title === 'Главная' ? 'bg-white/10 text-white font-medium' : 'text-slate-400 hover:text-white transition-colors' %>">
            <span class="material-icons-round text-lg">grid_view</span>
            Главная
          </a>
          <a href="/cases" class="px-5 py-2 rounded-full text-sm flex items-center gap-2 min-w-max <%= title === 'Кейсы' ? 'bg-white/10 text-white font-medium' : 'text-slate-400 hover:text-white transition-colors' %>">
            <span class="material-icons-round text-lg">shopping_bag</span>
            Кейсы
          </a>
          <% if (viewUser && (viewUser.level === 'leadership' || viewUser.level === 'dev')) { %>
            <a href="/admin" class="px-5 py-2 rounded-full text-sm flex items-center gap-2 min-w-max <%= title === 'Админ панель' ? 'bg-white/10 text-white font-medium' : 'text-slate-400 hover:text-white transition-colors' %>">
              <span class="material-icons-round text-lg">security</span>
              Админ
            </a>
          <% } %>
        </div>
      </div>
      <% if (viewUser) { %>
      <div class="flex items-center gap-4">
        <button id="open-mobile-nav" class="md:hidden w-10 h-10 flex items-center justify-center rounded-full bg-white/5 hover:bg-white/10 transition-all border border-white/5">
          <span class="material-icons-round">menu</span>
        </button>
        <div class="flex flex-col items-end mr-2">
          <span class="text-xs text-slate-400 font-medium">Баланс</span>
          <span class="text-sm font-bold text-white flex items-center gap-1">
            <span id="nav-balance"><%= (viewUser.balance || 0).toLocaleString('ru-RU') %></span>
            <span class="material-icons-round text-primary text-xs">toll</span>
          </span>
        </div>
        <div class="flex items-center gap-3 bg-white/5 hover:bg-white/10 transition-colors p-1.5 pr-4 rounded-full border border-white/5">
          <button class="flex items-center gap-3" data-action="open-profile" title="Профиль">
            <% if (viewUser.avatarUrl) { %>
            <img class="w-8 h-8 rounded-full object-cover" src="<%= viewUser.avatarUrl %>" alt="<%= viewUser.username %>" referrerpolicy="no-referrer">
            <% } else { %>
            <div class="w-8 h-8 rounded-full bg-gradient-to-tr from-primary to-accent-purple flex items-center justify-center text-xs font-bold"><%= (viewUser.username || '').slice(0, 2).toUpperCase() %></div>
            <% } %>
            <span class="text-sm font-semibold"><%= viewUser.username %></span>
          </button>
        </div>
        <button id="logout-btn" class="w-10 h-10 flex items-center justify-center rounded-full bg-white/5 hover:bg-red-500/20 hover:text-red-400 transition-all border border-white/5">
          <span class="material-icons-round">logout</span>
        </button>
      </div>
      <% } %>
    </div>
  </nav>
  <% } %>

  <main class="max-w-7xl mx-auto px-6 py-8">
    <%- body %>
  </main>

  <div class="modal fixed inset-0 items-center justify-center px-4 bg-black/60 z-50" id="case-spinner" aria-hidden="true">
    <div class="w-full max-w-xl bg-surface-dark border border-white/10 rounded-3xl p-6 glass relative">
      <button class="absolute top-3 right-3 w-9 h-9 rounded-2xl border border-white/10 bg-white/5 text-slate-200 flex items-center justify-center hover:bg-red-500/20 hover:text-red-400 transition-colors" data-close aria-label="Закрыть">×</button>
      <button class="absolute top-3 right-14 inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-white/10 text-slate-200 hover:text-white hover:bg-white/10 text-sm md:text-base font-semibold transition-colors" id="show-prizes" title="Призы">
        <span class="material-icons-round text-sm">list_alt</span>
        Призы
      </button>
      <div class="flex items-center gap-3 mb-4">
        <div class="w-10 h-10 rounded-2xl bg-primary/10 text-primary flex items-center justify-center">
          <span class="material-icons-round">stars</span>
        </div>
        <div>
          <h2 class="text-lg font-semibold text-white mb-1">Открытие кейса</h2>
          <p class="text-xs text-slate-400 mb-0">Удачи! 🎁</p>
        </div>
      </div>
      <div class="spinner-track border border-white/10 rounded-2xl overflow-hidden bg-black/30 relative">
        <div class="spinner-pointer"></div>
        <div class="spinner-reel" id="spinner-reel"></div>
      </div>
      <div class="mt-4" id="spinner-result"></div>
      <div class="spinner-prizes-panel hidden mt-4" id="spinner-prizes-panel"></div>
    </div>
  </div>

  <div class="modal" id="mobile-nav" aria-hidden="true">
    <div class="modal-content">
      <button class="modal-close" data-close aria-label="Закрыть">×</button>
      <div class="space-y-3">
        <a href="/app" class="btn btn-secondary btn-pill w-full flex items-center justify-between">
          <span class="flex items-center gap-2"><span class="material-icons-round">grid_view</span>Главная</span>
          <span class="material-icons-round">arrow_forward</span>
        </a>
        <a href="/cases" class="btn btn-secondary btn-pill w-full flex items-center justify-between">
          <span class="flex items-center gap-2"><span class="material-icons-round">shopping_bag</span>Кейсы</span>
          <span class="material-icons-round">arrow_forward</span>
        </a>
        <% if (viewUser && (viewUser.level === 'leadership' || viewUser.level === 'dev')) { %>
        <a href="/admin" class="btn btn-secondary btn-pill w-full flex items-center justify-between">
          <span class="flex items-center gap-2"><span class="material-icons-round">security</span>Админ</span>
          <span class="material-icons-round">arrow_forward</span>
        </a>
        <% } %>
      </div>
    </div>
  </div>

  <script src="/app.js" defer></script>
  <% if (title === 'Админ панель') { %>
  <script src="/admin.js" defer></script>
  <% } %>
  <div id="particle-layer" class="particle-layer" aria-hidden="true"></div>

  <div class="modal" id="profile-modal" aria-hidden="true">
    <div class="modal-content">
      <button class="modal-close" data-close aria-label="Закрыть">×</button>
      <div id="profile-detail"></div>
    </div>
  </div>
</body>
</html>
